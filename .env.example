# Auth.js / NextAuth v5 — empty placeholders only. Copy to .env.local.
# Do not commit real values. Never commit AUTH_GOOGLE_SECRET or AUTH_SECRET.
# Generate AUTH_SECRET locally with: npx auth secret
#
# Google Cloud project: SIRAGPT (gen-lang-client-0149080720)
# Consent screen: app name Dolarnett, External / testing
# Test user: carrerajorge874@gmail.com
# Authorized JavaScript origin (local): http://localhost:3000
# Authorized redirect URI (local): http://localhost:3000/api/auth/callback/google
# For production, add the deployed origin and https://<host>/api/auth/callback/google
#
# Local AUTH_URL is typically http://localhost:3000

AUTH_SECRET=
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
AUTH_URL=

# FX: open feed is used by default (no key).
# Optional paid/key feed:
# EXCHANGE_RATE_API_KEY=
# Commission in PEN added on top of the send amount (default 0):
# COMMISSION_PEN=0
